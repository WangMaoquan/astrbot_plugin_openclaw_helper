{
  "whitelist": {
    "description": "List of user IDs allowed to execute dangerous commands",
    "type": "list",
    "default": []
  },
  "dangerous_keywords": {
    "description": "Keywords that trigger dangerous operation warning",
    "type": "list",
    "default": ["删除", "rm ", "rm -rf", "exec", "sudo", "shutdown", "reboot", "kill"]
  },
  "enabled": {
    "description": "Enable or disable the plugin",
    "type": "bool",
    "default": true
  }
}
